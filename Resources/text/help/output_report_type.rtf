<h2>Output report type</h2>
Select either CSV or XLSX.

<h3>Recommendation</h3>
CSV is the recommended export format in most cases. This is because CSV files are much faster to generate than XLSX (processing time reduced from hours to seconds/minutes).
